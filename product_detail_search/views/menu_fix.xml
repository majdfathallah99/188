<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- One canonical client action -->
  <record id="action_product_detail_search" model="ir.actions.client">
    <field name="name">Find Product</field>
    <field name="tag">product_detail_search_barcode_main_menu</field>
    <field name="type">ir.actions.client</field>
    <!-- OPTIONAL but helpful: a context so we can detect it later -->
    <field name="context">{'from_product_detail_search': True}</field>
  </record>

  <!-- One canonical menu that points to that action -->
  <menuitem id="menu_product_detail_search"
            name="Find Product"
            parent="stock.menu_stock_root"
            action="action_product_detail_search"
            sequence="90"/>
</odoo>
